[package]
name = "kols-store"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
kols-core = { path = "../kols-core" }
intranet-identity.workspace = true
rusqlite = { version = "0.37", features = ["bundled"] }

[dev-dependencies]
